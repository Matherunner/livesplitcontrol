{
  "name": "timer",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://matherunner.github.io/livesplitcontrol",
  "dependencies": {
    "query-string": "^6.1.0",
    "react": "^16.4.2",
    "react-dom": "^16.4.2"
  },
  "devDependencies": {
    "gh-pages": "^1.2.0",
    "react-scripts": "^1.1.4"
  },
  "scripts": {
    "build:core": "./build-core.sh",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "yarn build",
    "deploy": "gh-pages -d build"
  }
}
